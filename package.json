{
  "name": "dispatch-agent",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r dev",
    "deploy": "pnpm --filter infra deploy",
    "destroy": "pnpm --filter infra destroy"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "@types/node": "^20.10.5"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}