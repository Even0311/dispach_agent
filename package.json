{
  "name": "dispatch-agent",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r dev",
    "deploy": "pnpm --filter infra deploy",
    "destroy": "pnpm --filter infra destroy"
  },
  "devDependencies": {
    "@types/node": "^20.10.5",
    "esbuild": "^0.25.10",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}